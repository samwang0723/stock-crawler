version: '3'
services:
  kafka:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: kafka
    user: root
    image: samwang0723/kraft-kafka:v1
    environment:
      - KAFKA_CREATE_TOPICS=kafka-test:3:1
    ports:
      - "9092:9092"
